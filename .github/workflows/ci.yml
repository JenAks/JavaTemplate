name: Java CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: mvn dependency:resolve
        run: mvn dependency:resolve

      - name: seal cli
        uses: seal-community/cli-action@latest
        with:
          mode: fix
          fix_mode: local
          token: ${{ secrets.SP_TOKEN }}
          project: 7EABB682929998CA3D0D4CE260ABEF64E281E7BD
          target: pom.xml

      - name: mvn install
        run: mvn install

      - name: mvn package
        run: mvn package

      - name: mvn verify
        run: mvn verify
